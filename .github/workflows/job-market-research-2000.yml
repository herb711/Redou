name: job-market-research-2000

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow temporarily paused while sharded research jobs are prepared."
